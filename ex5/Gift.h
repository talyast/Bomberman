//#pragma once
//#include "StaticObject.h"
//class Gift:public StaticObject
//{
//	public:
//
//		Gift(sf::Vector2f location);
//		void draw(sf::RenderWindow &m_window);
//		virtual todo handleCollision(Objects &Obj)override { return NOTHING;};
//		virtual todo handleCollision(Door &Obj)override { return NOTHING; };
//		virtual todo handleCollision(Robot &Obj)override { return NOTHING; };
//		virtual todo handleCollision(Guard &Obj)override { return NOTHING; };
//		virtual todo handleCollision(Wall &Obj)override {return NOTHING;};
//		virtual todo handleCollision(Bomb &Obj) override { return NOTHING; };
//		virtual todo handleCollision(Gift &Obj)override { return NOTHING ; };
//		virtual todo handleCollision(Rock &Obj)override { return NOTHING; };
//		/*virtual todo handleCollision(SmartGuard &Obj)override { return NOTHING; };
//		virtual todo handleCollision(RandomGuard &Obj)override { return NOTHING; };*/
//		void setToInitPosition();
//
//	private:
//		sf::Vector2f m_location;
//		sf::Texture  m_texture;
//	};
//
//
//
//
